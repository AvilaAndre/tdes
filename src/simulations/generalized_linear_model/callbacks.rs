use crate::internal::{context::Context, message::Message};

pub fn _example_on_message_receive(
    _ctx: &mut Context,
    _receiver_id: usize,
    _msg: Option<Box<dyn Message>>,
) {
    todo!();
}
